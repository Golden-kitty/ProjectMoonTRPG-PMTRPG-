# 全量 canonical HTML 发布验收（2026-09-08）

## 任务与授权

- TaskType：既有全量表格改造的收口、分发验证与 edit 分支交付。
- Goal：370 张正文表统一 canonical HTML，保留非表格 Markdown 正文；补齐 CI，按源文清单生成 CHM、DOCX、PDF，完成 edit 提交/推送及站点前后对比。旧计数 369 漏掉了“休整”中的单列表，最终清单包含该表。
- OutOfScope：重写规则机制、数值或术语；修改原始 PDF/CHM；纳入个人配置和技能平衡草稿；未经允许变更线上部署分支。
- EditableAreas：93 个已确认正文文件、转换/导出/校验脚本、CSS、CI、分发依赖与相应验收记录。
- ForbiddenAreas：`originFab/`、既有 stash、个人配置、无关草稿；生成物不得强制加入 Git。
- Provides：单次 edit 交付提交、12 个互不重叠的文档级回滚清单、站点前后证据、三种二进制成品及来源哈希清单。
- AcceptanceChecks：正文表覆盖及 ID/span/嵌套关系一致；源文差异可追溯；图标完整；DOCX 可见边框与排版检查；PDF 字体嵌入及页面检查；CHM 编译/解包/查看器检查；CI 与推送状态分别记录。

用户于 2026-09-08 明确授权在完成改造后于 `Halcyon'edit` 做一次提交和推送。因此本次采用一个原子交付提交及 12 个文档互斥清单作为等价可恢复边界，而非伪造 12 次已经发生的独立提交。每批恢复来源是提交父节点 `d3fc747d6ad36c0d8cba61fda37d19246482bfe3` 及 manifest 中的精确文档路径。恢复前必须确认相关路径没有后续编辑；有后续编辑时用受审差异补丁，不能覆盖用户工作。

## 提交前新增的正文回归修复

独立审计发现历史候选中有四个未记录的可见文本变化。逐项比对 HEAD、`0ca326d`、保护 stash 及 stash 父提交，确认它们并非本次有权顺带发布的规则变化。仅修复以下文本，所有表格 ID、行列和 span 保持原状：

- `标准格斗术.md` / `html-74dd252d21f5`：动作格恢复“计划”，效果行保留“从这两个骰中选择一个部署”，避免重复占格。
- `街头搏击术.md` / `html-5a2e3ea3936d`：招架效果恢复当地地头蛇／搏斗条件的威力加值；下方突刺的流血效果不变。
- `丧家犬.md` / `html-bab1acb4c2ff`：恢复“笼中狗斗”原表，22–44、声望 40+ 等原文不再被后表“爆裂打击”重复副本覆盖；后一表外标题恢复“再来再来”。
- `臼齿事务所.md` / `html-0bb68df01416`：表内恢复原“链而走险”，外标题仍为“铤而走险”；保留原始名称不一致，不借格式迁移擅自裁定。

四个文件修复后去格式可见文本与 HEAD 完全一致，结构属性序列与修复前一致；新增四项精准回归测试。修复前全文按原路径保存在 `output/evidence/html-release-20260908/source-before-repair/`。只有本记录列出的差异核对完成后，才同步清单与批次文件哈希。

其余非纯格式差异沿用既有已确认范围：默认技能“棍棒”、技能花色替换、已记录的晋升/同调/燃料/巨兵修正。它们不能描述为“只有格式变化”。

## 分发与站点检查范围

- 现有站点导航提供 203 个唯一 Markdown 路径；按已接受空壳页策略过滤 42 个分组页，实际输出 161 篇有正文文档，覆盖 92 篇表格文档中的全部 370 张正文表。
- 最终审阅批次：`output/export_samples/pmtrpg-html-review-20260908-v5/`，独立于已接受的 9 月 5 日试点，不覆盖旧试点；首次批次与 v2/v3/v4 均是保留的中间证据，不交付为最终稿。
- 本次移除站点和导出入口的隐式 semantic-table-merge 调用；规范源不再在渲染时被推断合并。
- 全量书籍补齐章节间链接、跨章节重复锚点隔离、CHM 包内链接及人类可读书名。源文不因此发生改写。
- 推送前线上成功部署来自 `hotFix` 的 `d3fc747`；204 个去重页面全部 HTTP 200，涵盖全部 92 篇表格正文；现网 365 张表、23 次图片引用，4 个唯一图片资源均可访问。
- 8 个代表页面在 1440px 与 390px 宽度下已保存 DOM 与截图，16 次检查均无页面级横向溢出。预期技能图标从旧版 23 次变为新版 42 次，不能用像素完全相同代替语义无损比较。

## 分发版面与解析修复

- “好棒棒快餐连锁”首个四级标题前补空行、去掉两个缩进空格，消除导出时露出的字面 `####`。逆向恢复这两项空白后精确命中原 SHA256；该文五表的结构和文字不变，清单定位、批次源哈希已同步。
- 属性平衡的 23 列表原本超出 PDF 可打印宽度并触发全书缩印。导出器对至少 16 列的宽表缩小横向留白和字号，使最右列及外框完整；普通正文恢复 12pt、普通表格恢复 9pt。不能用旧 PDF 页数和图像代替新稿验收。
- 标题、表题与短条目增加跟随后文的分页约束。长表仍允许跨页，Word 中仍有短卡表和效果行跨页、少量普通段落标签独立在页末的版面备注；没有将审阅稿等同于出版排版定稿。
- v4 逐页与原始 Markdown 复核发现 `attr_list` 把森语三篇中 20 段花括号提示吞作属性：车卡说明 13 段、战斗说明 4 段、非战斗说明 3 段。站点配置、配置生成器和导出器移除该扩展，三篇原文保持不动。当前正文没有依赖此扩展的合法属性列表；canonical 表格显式 HTML 属性仍保留。
- 已补按文档核对原始花括号提示的验证，避免把同一 Markdown 渲染器作为源与产物双方的唯一依据；不能用另一章节中的相同句子掩盖本章缺失。

## 提交前冻结的证据与门禁

证据根目录为 `output/evidence/html-release-20260908/`，只在本机及审阅包留存，不将生成物强制纳入 Git。下面是本次单次提交前的冻结记录；提交后 commit、远端 CI 和线上 after 的事实另写该目录及审阅包中的最终交付记录，避免为更新执行日志追加第二次提交。

| 检查 | 当前证据与结论 |
| --- | --- |
| 源与站点表结构 | canonical、清单、渲染双向覆盖均为 370 表、92 文档；逐表 ID、行列、跨度、嵌套关系检查通过。 |
| 分发结构 | DOCX 370 表、1995 个 gridSpan、114 个 vMerge；CHM 解包 161 篇、370 表、47 次图片引用、4 个图片文件。逐表几何与源一致。 |
| 原始文本完整性 | 花括号提示按原始源文独立核对；原有渲染后正文块、字体及图片检查保留。 |
| PDF 版面 | v5 共 275 页；v4 已逐页实看全部 275 页。严格 RGB 比较证明 v5 的 271 页与已看图像完全相同，变化的 268–271 页另行实看，20 段提示恢复。分页和窄列备注继续保留。 |
| Word 版面 | v5 DOCX 经 Microsoft Word 实际打印为 195 页。192 页与已逐页实看的 v4 完全像素一致，变化的 190–192 页另行实看，20 段提示恢复；短表/行内跨页仍是审阅备注。 |
| CHM 包内浏览器 | 实际解包的 161 篇在隔离 Chromium 中加载，370 表、47 次图片引用，0 坏图、0 页内失效锚点、0 页面级溢出。代表宽表/嵌套表/图标截图独立留存。 |
| CHM 原生查看器 | **PENDING**。原生截图 API 两次返回 `SetIsBorderRequired ... 0x80004002`；没有拿进程存在、编译成功或浏览器截图冒充 hh.exe 视觉通过。 |
| 测试与 CI | 仅安装 CI 依赖的隔离环境全套 94 项测试通过，包括 8 项原始花括号提示回归。远端首次冻结前的变化审计为 `BOOTSTRAP`，不是逐变化审查已经完成。明确补入 Linux CI 所需的 PyMuPDF。远端运行以推送后证据为准。 |
| 站点对比 | before 与 before-resume 已取证；本地代表页 1440px/390px 的 16 次采集无页面溢出、图片加载成功。edit 推送不触发 deploy-site；after 必须先核查线上部署 SHA，再比较同 URL/尺寸。 |
| 人类接受与生产部署 | **PENDING / 未授权**。全量审阅不继承 9 月 5 日试点的接受状态；本次只授权 edit 分支交付。 |

最终三种二进制 SHA256：

| 文件 | SHA256 |
| --- | --- |
| `pmtrpg-html-review-20260908-v5.pdf` | `708dd6c6c4341b271bd17f26660a1656f740efc7c478bff2de8e8bf4be791265` |
| `pmtrpg-html-review-20260908-v5.docx` | `fa9e09f9c4268e7e7fe9efc15a121f5d80f1212a54468d1212cfdf86800739ab` |
| `pmtrpg-html-review-20260908-v5.chm` | `924017dea6ea185a3c53137c3825575b4275a0dd2817685638fb1e31600f8e9e` |

Word 实际打印 PDF SHA256 为 `243afde91b5ae5269934e1cad0d0d5f1d87ce73bbf8ce3244ae8d8ab30bb91b1`。`release-manifest.json` 保留 161 篇实际构建输入的原始字节哈希；提交后固定来源时，还须逐篇核对 Git blob 与构建输入完整文本仅有 CRLF/LF 差异，并另记 Git blob SHA256，不伪称两种字节哈希完全相同。

## 保留项

`.codex/session-restore.md`、`AGENTS.md`、三份 `llm-skill-balance*` 草稿、全部 `output/` 与 `site/` 生成物均不纳入提交。保护 stash `5b4acbfc6b9c168df83ed3423463bdedd29cb159` 保持原样。
